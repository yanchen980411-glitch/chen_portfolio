# Prototype Instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

## Locked portfolio decisions

- All visible personal-name labels and page metadata use `Chen`; do not display `严琛` in the product UI.
- The project navigator is one true cube, never a triangular prism or a device mockup.
- Its four rotatable lateral faces are `01 AG1`, `02 S50C`, `03 TOOLS APP`, and `04 项目待更新`; the top and bottom faces stay neutral.
- The first three faces use the user's original project screenshots directly as textures. The fourth face is a code-native empty state and must never invent a project.
- The TOOLS face and its Section 00 fullscreen cover use the supplied final `1672 × 941` TOOLS poster unchanged as their shared static source. A single `ToolsAnimatedCover` supplies the same restrained 4-second SVG engineering-line overlay in both locations; the existing Three.js cube remains the underlying geometry/transition surface and must not be rebuilt.
- In Selected Works, the filled cube is deliberately oversized with a black solid body, but its complete 3D silhouette must remain inside the WebGL viewport at every supported breakpoint; no cuboid edge may be camera-clipped. Preserve that fit through the home camera/framing rather than exposing canvas overflow. Horizon and S50C use centered `cover` UVs; the approved TOOLS poster is the explicit exception and uses a left-aligned `cover` crop so its title and complete central floorplan survive the 1.6 face. Every screenshot fills its lateral face without stretching or repetition; detail-gallery screenshots remain uncropped with `contain`.
- Keep Selected Works intentionally stripped back around that focal cube: the previously removed bottom control group, upper-right index helper, prototype label, clock, and diagonal decoration stay absent; the left project-title rail and compact `01 / 04` metadata carry the visible navigation context.
- Chinese is the primary language. Preserve the locked references in `references/locked/v1/` as the visual source of truth.
- The MILESEEY Horizon case study is built chapter-by-chapter in this order: 项目概述; 背景&用户研究; User Journey&Pain Point; Design Principles&Information Architecture; Interaction Model&交互流程; User Flow; FOV/HUD布局; 设计探索&微交互; 最终方案.
- The Horizon project overview keeps the approved dark, cinematic composition: unchanged Chinese overview copy on the left, the supplied glasses render at the bottom right, and the supplied course map plus HUD projected outward above the lenses with clear perspective and green optical glow.
- Horizon project details use a persistent split layout on desktop: only the left case-study canvas scrolls through chapters, while the right project introduction remains fixed and unchanged.
- Horizon, S50C, and Tools detail entry each begins with a real Section 00 fullscreen cover that reuses the same `project.cover` source as the matching homepage cuboid face and spatial ENTER transition. Every entry must stop at the project scroll container's `scrollTop = 0`; no timer, snap, or automatic scrolling may reveal the existing case study. The unchanged case-study content follows immediately after the `100dvh` cover and appears only after deliberate wheel, trackpad, or touch scrolling. About Me remains outside this shared project-cover rule.
- Horizon, S50C, and Tools ENTER motion is one continuous 480ms power3-in-out-style transform/camera timeline: the selected face takes the shortest rotation path while the cuboid moves toward the camera and its outer stage expands to the viewport from the first frame. Keep the cuboid sides visible through roughly the first 90% and flatten depth only during the final handoff. Never insert an image-decode wait, `setTimeout`, large mid-motion mount/state change, or staged rotate-then-expand sequence; preload cover sources on the homepage, pre-mount the target detail invisibly, and hand off only after the transition surface fully matches the Section 00 cover. About Me, NEXT, and RETURN retain their existing behavior.
- The `Background & User Research` chapter follows the approved restrained case-study direction: black / black-green base, white and muted-grey typography, monochrome HUD Green accents, diagram-led comparison, thin dividers, and minimal surface treatments. If authentic research photography is unavailable, keep clearly labeled placeholders instead of sourcing or inventing documentary images.
- The approved `背景&用户研究.png` composition is now the source of truth for chapter 02: use its content and graphics directly in one viewport-height page, crop away the bottom quote and four summary icons, and do not render additional content beneath it.
- The MILESEEY Tools case study is built as one continuous white editorial page. Its detail shell reuses the exact S50C top navigation structure, classes, labels, and handlers for `/返回`, `下一个`, `严琛`, and `联系`; do not introduce Tools-specific navigation styling or behavior. Section 03, `From Insight to Structure`, must follow `/Users/yanchen/Desktop/流程/APP流程/03 From Insight to Structure.png` as its visual source of truth: preserve the exact copy order, three insight-to-structure mappings, project-centered relationship diagram, lifecycle hierarchy, and centered closing statement; do not add UI screenshots, phone mockups, cards, background decoration, or a subsequent section during this pass.
- Tools Section 01, `Project Overview`, follows `/Users/yanchen/Desktop/ChatGPT Image 2026年8月30日 17_43_18.png` for composition: keep the left editorial typography sparse; use exactly two independent real UI images (`tools-home-connected.png` and `tools-project-overview.png`), with Project Overview dominant and bottom/right cropped; use `floor-plan-workspace.png` only as a very faint background layer; omit the former overview description and third drawing-device mockup; keep the shared fullscreen cover, cube transition, right sticky project-info panel, prototype, and later case-study sections unchanged. On the `>1120px` desktop branch, the Project image must remain proportionally scaled (`height:auto`) and resolve to about `850px` unzoomed image height at a `1159px` viewport. Keep it slightly left of the former position, bottom-aligned to the composition, and below the foreground measurement/Home layers; the existing subtle rotated-edge crop is intentional.
- Tools Section 04, `Core Experience`, follows `/Users/yanchen/Desktop/流程/APP流程/04 Core Experience.png` as its visual source of truth: keep the three-stage left-copy/right-UI sequence, use only the supplied UI imagery, preserve 150–200px rendered breathing room between primary modules, keep the summary lightweight, and do not introduce Section 05 content.
- Tools Section 05, `Measure Smarter`, follows `/Users/yanchen/Desktop/流程/APP流程/05 Measure Smarter.png` as its visual source of truth: preserve the connected-measurement layering, the five-state drawing sequence, and the single row of eight supplied tool buttons; use the original UI PNGs, keep primary module spacing relaxed, omit a bottom summary, and stop before Section 06. On the `>1120px` desktop branch, keep the X Tape Mini popup only about `10px` farther right on screen than the earlier alignment; do not change its size, vertical position, or the surrounding connected-measurement composition.
- Tools Section 06, `Capture & Organize`, follows `/Users/yanchen/Desktop/流程/APP流程/06 Capture & Organize.png` as its visual source of truth: preserve the three-module capture/record/team sequence, use the supplied photo, save, Team, and Photos UI assets, keep Team dominant over Photos, retain the centered project summary, and stop before Final Experience.
- Tools Section 07, `Final Experience`, follows `/Users/yanchen/Desktop/流程/APP流程/07Final Experience.png` as its visual source of truth: keep the existing split shell and fixed project-info panel unchanged, use exactly five supplied real UI screens in the staggered Home / Project Overview / Photo Annotation / Device / Project Photos composition, preserve subtle phone framing and the centered connected-workspace closing statement, and do not add another section. On the `>1120px` desktop branch, all five screens and the complete closing statement must fit inside the visible left case-study pane without covering the section introduction. The Device and Project Photos screens should be uniformly enlarged relative to the other supporting screens, while retaining only a restrained overlap (about `48px` at the `1159 × 731` review viewport) and no image crop. The Tools right project-info panel intentionally ends after `ROLE` and `PROJECT`; do not restore the removed `SKILLS & DELIVERABLES` block.
- The S50C case study follows the Horizon project shell after its fullscreen Section 00 cover: keep the established `63.5% / 36.5%` desktop split, the pale-blue project-information panel on the right, the four top project controls, and all established case-study sections. The Section 00 cover itself uses the S50C homepage `project.cover` source with `object-fit: contain`; the existing section content beneath it must remain unchanged.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.
